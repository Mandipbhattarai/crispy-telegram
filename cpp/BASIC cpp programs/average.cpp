#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    int a, b, Average;
    cout << "Enter the value of a ";
    cin >> a;
    cout << "Enter the value of b ";
    cin >> b;
    Average = (a + b) / 2;
    cout << "The average of two numbers a and b is " << Average;
    
    return 0;
}
