#include <iostream>
using namespace std;

int main()
{
    int t, arr[t];
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        int a, b;
        cin >> a >> b;
        cout << a * b << endl;
    }

    return 0;
}